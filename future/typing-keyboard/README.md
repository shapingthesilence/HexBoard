# Deferred typing-keyboard feature

This directory preserves the typing-keyboard firmware, factory content, web
editor, and host tests for a possible later release. Nothing under this
directory is included by the normal firmware, factory-library, or web-app
builds.

The preserved code is a snapshot of the feature work and may need its current
settings/protocol integration restored when it is intentionally reintroduced.
